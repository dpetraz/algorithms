// strings are imutable - cannot change once created
// string has a length property
// strings are indexed (just like an array)

var speak = "Hello";

var letter = speak[4];

console.log(letter);

// we can use this control structure to parse through strings with for loops
